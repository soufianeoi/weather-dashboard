# OpenWeatherMap API key (required)
# Get yours at https://openweathermap.org/api
OWM_API_KEY=your_api_key_here

# Server host and port (optional, defaults below)
HOST=0.0.0.0
PORT=8000

# Secret for cache invalidation (optional, defaults to "dev-secret")
CACHE_SECRET=dev-secret
